{
  "name": "vpn-mock-mcp-server",
  "version": "1.0.0",
  "description": "Mock VPN gateway status server for ServiceNow AI Agent demo",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": { "node": ">=18.0.0" }
}
